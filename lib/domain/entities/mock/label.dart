import 'package:flutter/material.dart';
import 'package:github_issue_viewer/domain/entities/label.dart';

const mockLabel = Label(id: 'testLabel', name: 'a: build', color: Colors.amber);
const mockLabel2 =
    Label(id: 'testLabel2', name: 'a: video', color: Colors.purple);
